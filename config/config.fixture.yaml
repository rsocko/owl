source:
  mode: fixture
  fixture_path: ../tests/statements/fixtures/paperless_documents.json

analysis:
  min_documents_for_pattern: 3
  monthly_min_days: 25
  monthly_max_days: 35
  quarterly_min_days: 80
  quarterly_max_days: 100
  annual_min_days: 350
  annual_max_days: 380
  allowed_tags:
    - statement
    - bill
    - invoice
  minimum_title_consistency: 0.5
  default_grace_period_days: 5

runtime:
  snapshot_path: ../data/catalog.snapshot.json

server:
  host: 127.0.0.1
  port: 8001
